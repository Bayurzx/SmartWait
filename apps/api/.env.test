# Test Environment Configuration
NODE_ENV=test
PORT=3002

# Test Database Configuration (use a separate test database)
DATABASE_URL=postgresql://smartwait_user:smartwait_password@localhost:5432/smartwait
REDIS_URL=redis://localhost:6379/1

# JWT Configuration
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h

# Twilio SMS Configuration (use test credentials)
TWILIO_ACCOUNT_SID=test-account-sid
TWILIO_AUTH_TOKEN=test-auth-token
TWILIO_PHONE_NUMBER=+15551234567

# Staff Authentication
STAFF_USERNAME=testadmin
STAFF_PASSWORD=testpassword